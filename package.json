{
  "name": "reilist",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd src/frontend && npm run build && cd ../.. && rm -rf src/http/static/* && cp -r src/frontend/dist/* src/static/",
    "dev": "npx serve public -l 3000"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@architect/architect": "^11.2.2",
    "@architect/plugin-typescript": "^2.1.0",
    "@types/node": "^22.15.3"
  },
  "dependencies": {
    "@architect/functions": "^8.1.9",
    "@aws-sdk/client-dynamodb": "^3.797.0",
    "@aws-sdk/lib-dynamodb": "^3.797.0",
    "html-to-pdfmake": "^2.5.28",
    "pdfmake": "^0.2.20",
    "puppeteer": "^24.10.2",
    "typescript": "^5.8.3",
    "uuid": "^11.1.0"
  }
}
